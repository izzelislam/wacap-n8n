import { WacapConfig, SessionInfo, IStorageAdapter, IWASocket } from '../types';
import { EventManager } from '../events';
/**
 * WhatsApp session manager
 * Handles individual WhatsApp connections
 */
export declare class Session {
    private sessionId;
    private config;
    private socket;
    private eventManager;
    private storageAdapter;
    private sessionPath;
    private isConnecting;
    private shouldReconnect;
    private retryCount;
    constructor(sessionId: string, config: WacapConfig, storageAdapter: IStorageAdapter);
    /**
     * Start the WhatsApp session
     */
    start(): Promise<void>;
    /**
     * Connect to WhatsApp
     */
    private connect;
    /**
     * Setup event handlers for the socket
     */
    private setupEventHandlers;
    /**
     * Extract message body text
     */
    private getMessageBody;
    /**
     * Stop the session
     */
    stop(): Promise<void>;
    /**
     * Get session info
     */
    getInfo(): SessionInfo;
    /**
     * Get event manager for registering event handlers
     */
    getEventManager(): EventManager;
    /**
     * Get the socket instance
     */
    getSocket(): IWASocket | null;
    /**
     * Check if session is active
     */
    isActive(): boolean;
}
//# sourceMappingURL=session.d.ts.map